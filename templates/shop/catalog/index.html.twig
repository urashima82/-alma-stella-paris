{% extends 'shop/base.html.twig' %}

{% block title %}
    {% if activeCategory %}
        {{ activeCategory.name }} — Alma Stella Paris
    {% else %}
        Shop — Alma Stella Paris
    {% endif %}
{% endblock %}

{% block meta_description %}
    {% if activeCategory %}
        Discover our {{ activeCategory.name|lower }} collection. Curated stainless steel jewelry with natural stones.
    {% else %}
        Shop the full Alma Stella Paris collection. Curated stainless steel jewelry with natural stones, sourced between Paris and Mexico.
    {% endif %}
{% endblock %}

{% block body %}
    {# ── Page header ── #}
    <section class="bg-alma-surface">
        <div class="max-w-7xl mx-auto px-6 py-14 md:py-20 text-center">
            <h1 class="font-serif text-4xl md:text-5xl font-light text-alma-text mb-3">
                {% if activeCategory %}
                    {{ activeCategory.name }}
                {% else %}
                    The Collection
                {% endif %}
            </h1>
            <p class="font-sans text-sm text-alma-text-muted">
                {% if activeCategory %}
                    {{ activeCategory.nameFr }}
                {% else %}
                    Curated jewelry. Sourced globally. Worn daily.
                {% endif %}
            </p>
        </div>
    </section>

    <div class="max-w-7xl mx-auto px-6 py-10 md:py-16">
        {# ── Category filter ── #}
        <nav class="flex flex-wrap items-center justify-center gap-3 mb-12" aria-label="Category filter">
            <a href="{{ path('shop_catalog') }}"
               class="px-4 py-2 text-sm font-sans transition-colors border
                      {{ activeCategory is null
                          ? 'bg-alma-text text-alma-bg border-alma-text'
                          : 'text-alma-text-muted border-alma-hover hover:border-alma-gold hover:text-alma-text' }}">
                All
            </a>
            {% for category in categories %}
                <a href="{{ path('shop_catalog', {categorySlug: category.slug}) }}"
                   class="px-4 py-2 text-sm font-sans transition-colors border
                          {{ activeCategory is not null and activeCategory.id == category.id
                              ? 'bg-alma-text text-alma-bg border-alma-text'
                              : 'text-alma-text-muted border-alma-hover hover:border-alma-gold hover:text-alma-text' }}">
                    {{ category.name }}
                </a>
            {% endfor %}
        </nav>

        {# ── Product grid ── #}
        {% if pagination|length > 0 %}
            <div class="grid grid-cols-2 md:grid-cols-3 gap-6 md:gap-8">
                {% for product in pagination %}
                    <article class="group">
                        <a href="{{ path('shop_product', {slug: product.slug}) }}" class="block cursor-pointer">
                            <div class="relative aspect-[4/5] bg-alma-surface overflow-hidden
                                        border border-transparent group-hover:border-alma-gold
                                        transition-colors duration-300 flex items-center justify-center
                                        {{ product.isSoldOut ? 'opacity-60 grayscale' : '' }}">
                                {% if product.images is not empty %}
                                    <img src="{{ asset('uploads/products/' ~ product.images.first.filename) }}"
                                         alt="{{ product.name }}"
                                         class="w-full h-full object-cover
                                                group-hover:scale-105 transition-transform duration-500"
                                         loading="lazy">
                                {% else %}
                                    <div class="text-center">
                                        <p class="text-alma-gold text-3xl mb-2">✦</p>
                                        <p class="text-xs text-alma-text-muted">{{ product.name }}</p>
                                    </div>
                                {% endif %}
                                {# Badges #}
                                <div class="absolute top-2 left-2 flex flex-col gap-1">
                                    {% if product.isSoldOut %}
                                        <span class="px-2 py-0.5 bg-alma-text text-alma-bg text-[10px] font-sans font-medium">Vendue</span>
                                    {% else %}
                                        <span class="px-2 py-0.5 bg-alma-gold text-white text-[10px] font-sans font-medium">Pièce unique</span>
                                    {% endif %}
                                    {% if product.isNew() %}
                                        <span class="px-2 py-0.5 bg-alma-surface text-alma-text text-[10px] font-sans font-medium border border-alma-hover">Nouvelle</span>
                                    {% endif %}
                                </div>
                            </div>
                            <div class="mt-3 flex justify-between items-start gap-2">
                                <div>
                                    <h3 class="font-serif text-base text-alma-text group-hover:text-alma-gold transition-colors">
                                        {{ product.name }}
                                    </h3>
                                    <p class="text-alma-text-muted text-xs mt-0.5">
                                        {{ product.category.name }}
                                    </p>
                                </div>
                                <p class="font-sans font-semibold text-alma-text whitespace-nowrap">
                                    {% if product.isSoldOut %}
                                        <span class="text-alma-text-muted text-xs">Sold</span>
                                    {% else %}
                                        ${{ product.displayPrice | number_format(0) }}
                                    {% endif %}
                                </p>
                            </div>
                        </a>
                    </article>
                {% endfor %}
            </div>

            {# ── Pagination ── #}
            {% if pagination.pageCount > 1 %}
                <nav class="mt-16 flex items-center justify-center gap-2" aria-label="Pagination">
                    {# Previous #}
                    {% if pagination.currentPageNumber > 1 %}
                        <a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({page: pagination.currentPageNumber - 1})) }}"
                           class="px-4 py-2 text-sm font-sans border border-alma-hover text-alma-text-muted
                                  hover:border-alma-gold hover:text-alma-text transition-colors">
                            &larr; Previous
                        </a>
                    {% endif %}

                    {# Page numbers #}
                    {% for page in 1..pagination.pageCount %}
                        {% if page == pagination.currentPageNumber %}
                            <span class="px-4 py-2 text-sm font-sans bg-alma-text text-alma-bg border border-alma-text">
                                {{ page }}
                            </span>
                        {% else %}
                            <a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({page: page})) }}"
                               class="px-4 py-2 text-sm font-sans border border-alma-hover text-alma-text-muted
                                      hover:border-alma-gold hover:text-alma-text transition-colors">
                                {{ page }}
                            </a>
                        {% endif %}
                    {% endfor %}

                    {# Next #}
                    {% if pagination.currentPageNumber < pagination.pageCount %}
                        <a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({page: pagination.currentPageNumber + 1})) }}"
                           class="px-4 py-2 text-sm font-sans border border-alma-hover text-alma-text-muted
                                  hover:border-alma-gold hover:text-alma-text transition-colors">
                            Next &rarr;
                        </a>
                    {% endif %}
                </nav>
            {% endif %}

        {% else %}
            {# ── Empty state ── #}
            <div class="text-center py-20">
                <p class="text-alma-gold text-4xl mb-4">✦</p>
                <p class="font-serif text-2xl text-alma-text mb-2">No pieces found</p>
                <p class="text-sm text-alma-text-muted mb-8">
                    {% if activeCategory %}
                        No published products in this category yet.
                    {% else %}
                        Our collection is being curated. Check back soon.
                    {% endif %}
                </p>
                {% if activeCategory %}
                    <a href="{{ path('shop_catalog') }}"
                       class="inline-block border border-alma-gold text-alma-gold px-8 py-3 font-sans text-sm
                              hover:bg-alma-gold hover:text-white transition-colors">
                        View all jewelry
                    </a>
                {% endif %}
            </div>
        {% endif %}
    </div>
{% endblock %}
