framework:
    rate_limiter:
        contact_form:
            policy: fixed_window
            limit: 3
            interval: '15 minutes'
        admin_login:
            policy: sliding_window
            limit: 3
            interval: '15 minutes'
        registration:
            policy: fixed_window
            limit: 5
            interval: '30 minutes'
        checkout:
            policy: fixed_window
            limit: 10
            interval: '15 minutes'
